#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int my_sumList(vector<int> l){
    if (l.size() == 0){
        return 0;
    } else {

    }
}

int main() {
    vector<int> l = {3, 1, 2, 3};
}